/**
 * Curated source links for the hero's media pool. Each entry is a normal,
 * shareable page URL (not an embed/iframe URL), resolved server-side into
 * a direct image URL via lib/media/resolve.ts.
 *
 * Add more links any time, the pool degrades gracefully: a broken or
 * removed link is just skipped, not a build failure.
 */

export const YOUTUBE_VIDEO_URLS: string[] = [
  // e.g. 'https://www.youtube.com/watch?v=XXXXXXXXXXX',
]

export const FLICKR_PHOTO_URLS: string[] = [
  // e.g. 'https://www.flickr.com/photos/mayeulchassagnard/XXXXXXXXXXX/',
]

export const INSTAGRAM_POST_URLS: string[] = ['https://www.instagram.com/p/CFEPZpnnDpT/']
